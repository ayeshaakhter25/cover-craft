const express = require('express');&#10;const request = require('supertest');&#10;const app = express();&#10;app.use(express.json());&#10;&#10;// Mock services&#10;jest.mock('./src/services/resume.service');&#10;jest.mock('./src/services/job.service');&#10;jest.mock('./src/services/skill.service');&#10;&#10;const ResumeService = require('./src/services/resume.service');&#10;const JobService = require('./src/services/job.service');&#10;const SkillService = require('./src/services/skill.service');&#10;const matchController = require('./src/controllers/match.controller');&#10;&#10;// Mock data&#10;const mockResumeSkills = ['React', 'JavaScript', 'Python'];&#10;const mockJobSkills = ['React', 'JavaScript', 'Docker', 'AWS'];&#10;&#10;describe('Match Score API', () => {&#10;    beforeEach(() => {&#10;        jest.clearAllMocks();&#10;        ResumeService.extractResumeWithSkills.mockResolvedValue({&#10;            skills: mockResumeSkills&#10;        });&#10;        JobService.getJobDescription.mockResolvedValue('mock job desc with React JavaScript Docker AWS');&#10;        SkillService.extractSkills.mockImplementation(text => &#10;            text.includes('React') ? ['React'] : []&#10;        );&#10;        SkillService.calculateMatchScore.mockReturnValue({&#10;            matchScore: 75,&#10;            matchingSkills: ['React', 'JavaScript'],&#10;            missingSkills: ['Docker', 'AWS']&#10;        });&#10;    });&#10;&#10;    test('should calculate match score from resume file and job file', async () => {&#10;        const response = await request(app)&#10;            .post('/api/match-score')&#10;            .send({&#10;                resumeFile: 'test-cv.pdf',&#10;                jobFile: 'test-jd.txt'&#10;            })&#10;            .expect(200);&#10;&#10;        expect(response.body.matchScore).toBe(75);&#10;        expect(response.body.matchingSkills).toEqual(expect.arrayContaining(['React']));&#10;        expect(response.body.missingSkills).toEqual(expect.arrayContaining(['Docker']));&#10;    });&#10;&#10;    test('should calculate match score from resume file and direct job desc', async () => {&#10;        const response = await request(app)&#10;            .post('/api/match-score')&#10;            .send({&#10;                resumeFile: 'test-cv.pdf',&#10;                jobDesc: 'requires React, Docker'&#10;            })&#10;            .expect(200);&#10;&#10;        expect(response.body.matchScore).toBeGreaterThan(0);&#10;    });&#10;});&#10;&#10;console.log('✅ Match score module implemented successfully!');&#10;console.log('📋 Test with: POST http://localhost:5000/api/match-score');&#10;console.log('📝 Body: { "resumeFile": "cv_xxx.pdf", "jobFile": "jd_xxx.txt" }');
